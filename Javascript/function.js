function increment(num) {
    var ans = num+10;
    return ans;
}
// return type is not necessary
// it also not mandatory to specity parameter type in the function operator

var temp = increment(11);
console.log(temp);